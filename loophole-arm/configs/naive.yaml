# Naive reward: optimizes only peak cup height.
# Watch the arm learn to throw the cup.
reward: naive_peak_height

env:
  n_waypoints: 6
  sim_seconds: 3.0

optimizer:
  population: 32
  elite: 8
  sigma: 0.6
  sigma_decay: 0.97
  init_scale: 0.3
  generations: 60
  seed: 1

output_dir: runs
