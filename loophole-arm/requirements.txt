# requirements.txt — pinned dependencies for reproducible deployment.
#
# Generated from: pip freeze (Python 3.12, Linux x86_64)
# Last updated: 2026-06-26
#
# Install:
#   pip install -r requirements.txt
#
# For real-hardware (Feetech + LeRobot):
#   pip install -r requirements.txt -r requirements-hardware.txt
#
# For development:
#   pip install -r requirements.txt -r requirements-dev.txt

# ── Core sim layer ─────────────────────────────────────────────────────
mujoco==3.10.0
numpy==2.4.4
imageio==2.37.3
imageio-ffmpeg==0.6.0
PyYAML==6.0.3

# ── Inverse kinematics (mink, MuJoCo-native differential IK) ───────────
mink==1.2.0
qpsolvers==4.12.0
daqp==0.8.7

# ── MuJoCo transitive deps ─────────────────────────────────────────────
absl-py==2.4.0
etils==1.14.0
glfw==2.10.0
PyOpenGL==3.1.10
pillow==12.1.1

# ── The package itself ─────────────────────────────────────────────────
# Install with: pip install -e .  (editable, for development)
# OR:           pip install .     (regular install)
